(*
 * calculeaza h(n) = 1 + 1/2 + 1/3 +...+ 1/n
 *)

program egwhile;

var
  n : integer;
  h : real;

begin
  n := readInteger;
  writeInteger(n);
  h := 0;
  while n>0 do
  begin
    h := h + 1/n;
    n := n - 1;
  end;
  writeReal(h);
end.